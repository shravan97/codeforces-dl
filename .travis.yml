sudo: required
language: python
python:
  - "2.6"
  - "2.7"
before_install:
  - sudo apt-get update
  - sudo apt-get install wkhtmltopdf
install:
  - pip install setuptools --upgrade
  - python setup.py install
script:
  nosetests